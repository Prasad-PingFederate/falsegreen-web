# Test-only dependencies. Kept out of requirements.txt so the runtime image
# stays minimal - the Dockerfile installs that file, not this one.
-r requirements.txt

pytest==9.1.1
playwright>=1.40.0
pytest-playwright>=0.4.0

# The analysis engine, from the repository that actually contains it.
falsegreen @ git+https://github.com/Prasad-PingFederate/falsegreen@90013e44b203cd6a10495db73b7378a2b7d48b2a
